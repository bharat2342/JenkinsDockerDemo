pipeline {
    agent none
    stages {
        stage('Build') {
            agent {
                docker {
                    image 'maven:3.8.7'
                    // You can also specify other Docker configurations if needed
                    // e.g., args '-u root:root'
                    //      registryUrl 'https://your-registry.example.com'
                    //      registryCredentialsId 'your-registry-credentials-id'
                }
            }
            steps {
                // Your build steps here
                // e.g., sh 'npm install' or 'mvn clean install'
            }
        }
    }
}
